# Use the official Jenkins LTS base image
FROM jenkins/jenkins:lts

# Switch to root user to install additional tools if needed
USER root

# (Optional) Install extra tools, plugins, or packages here
# Example: RUN apt-get update && apt-get install -y git

# Switch back to the Jenkins user
USER jenkins